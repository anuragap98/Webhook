[tool.black]
line-length = 88
target-version = ["py310"]

[tool.ruff]
line-length = 88
# Ignore venv and migrations generated files
exclude = [".venv", "*/migrations/*"]

[tool.isort]
profile = "black"
